# Dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment
.env
.env.local
.env.*.local

# PHP config (contains real credentials — never commit)
config.php

# Build & Dist
dist/
build/
.next/
out/

# Screenshots & Temporary Files
temporary screenshots/
design_ideas/
notes.txt

# Puppeteer Cache
.cache/
.puppeteer/

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*.sublime-workspace
.DS_Store

# OS
Thumbs.db
.DS_Store
desktop.ini

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local development
.local
.env.development.local

# Admin panel (separate deployment, not part of main site)
fbi_admin_page/

# Scraped competitor/reference page dumps (local research only, never publish)
scraper-reports/

# Generated oversize fixtures for upload-cap testing
test-oversize-*.pdf

# Order capture log — real customer PII and backed-up identity documents.
# Never commit. (The .htaccess inside it IS tracked, via the negation below.)
order_records/*
!order_records/.htaccess
!order_records/index.html

# Temporary mail diagnostic — not part of the site
mail-test.php

# Temporary read-only verifier for E2E testing
order-verify.php
